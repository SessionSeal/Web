{
  "name": "sessionseal-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start -p 3001",
    "deploy": "npm run build && (pm2 delete web || true) && pm2 start \"npm run start\" --name web && pm2 save"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "next": "^15.4.5",
    "next-auth": "^5.0.0-beta.32",
    "pg": "^8.23.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
